import React from "react";
import { Section, Container } from "../UI/Public";

const Feedback = () => {
  return (
    <Section>
      <Container widther>Feedback</Container>
    </Section>
  );
};

export default Feedback;
