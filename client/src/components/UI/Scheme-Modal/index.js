import React from "react";
import { ModalWrapper } from "./SchemeModal";

const SchemeModal = (props) => {
  return <ModalWrapper isOpen={props.isOpen}>123</ModalWrapper>;
};

export default SchemeModal;
