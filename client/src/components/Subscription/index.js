import React from "react";
import { Section, Container } from "../UI/Public";
import { Card, CardWrapper } from "./Subscription";

const Subscription = () => {
  return <CardWrapper>Subscription</CardWrapper>;
};

export default Subscription;
